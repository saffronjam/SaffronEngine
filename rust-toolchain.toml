# The saffron-build toolbox ships rustc/cargo 1.96.0 as Fedora packages (no rustup), so this pin
# takes effect only under a rustup-managed toolchain. rustfmt + clippy back `just lint` / `just format`.
[toolchain]
channel = "1.96.0"
components = ["rustfmt", "clippy"]
profile = "minimal"
